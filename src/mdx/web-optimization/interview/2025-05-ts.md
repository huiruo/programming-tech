1. 从简历开始聊.

我刚开始当面试官的时候也差不多，不知道该问啥，自己找了一堆八股题挨个顺序问。。。面的多了就有点儿经验了，一般就先和对方聊一聊项目经历，就算自己不懂对方的项目也没关系，让他先给自己介绍一下，然后问他项目的主要流程、架构设计，再挑出自己觉得可能是关键的地方详细问问是如何思考的。然后再问问一些经典题目

https://www.v2ex.com/t/971433

看看他写了啥,问一些过往使用过的技术问题.并根据他的回答,由浅入深.比如:做了什么-->怎么做的-->为什么这么做-->原理是什么.

先介绍下公司,优点从不加班,月薪不如互联网行业，先让候选人有个大致印象

2. 从需求开始聊. 从工作需要的技术进行沟通.

- 弄清楚你公司招人需要解决什么问题，解决这些问题需要什么知识储备，围绕这些知识储备设计提问。这是核心
然后就看面试过程中，面试者能否理解提问并且清楚的表达出自己观点。这个是涉及到以后工作中，能否愉快的沟通。

- 主观题 就是聊一聊以往项目遇到的难点是怎么解决的，技术栈的选取，项目管理/团队合作的理念、方法，个人的职业规划，技术栈兴趣等等

- 头一次面试一定要多看几遍候选人的简历，对自己准备的客观题一定要有准确的答案，主观题一定要有自己一套基本的看法。

- `我面试人，经常考虑入职了跟团队能否和谐相处`

- `当你问一个问题，面试者答完了，你就问，还有吗`

- 基本流程：八股文——项目——问题，如果之前的笔试没考算法，那看时间最后可以来几道

- 总体看下来都是和面试官能够流畅沟通，表达能力优秀的~

## 问题
- 公司几个前端？开发组织架构大概是怎么样的，你在当中担任什么角色？

- 公司产品

- 离职原因

- 平时数据结构/算法在前端代码的运用

- 这一岗位的期望是什么?

- 你有什么要问我的吗?

## TypeScript 类型
布尔类型（boolean）
数字类型（number）
字符串类型(string)
数组类型（array）
元组类型（tuple）
枚举类型（enum）
任意类型（any）
null 和 undefined
void类型
never类型

- unknown 类型
unknown 和 any类型的区别
```
类型安全：
any：使用 any 类型会关闭 TypeScript 的类型检查，因此您可以对任何类型的值执行任何操作，而不会得到编译时类型检查错误。这意味着您失去了类型的安全性。
unknown：使用 unknown 类型保留了类型安全性。当您使用 unknown 类型时，您必须进行类型检查或类型断言，以在运行时访问其值，这有助于避免在编译时出现类型错误。
类型推断：
any：any 类型的值可以赋给任何其他类型，而且它不会触发类型推断。这意味着当您使用 any 类型时，TypeScript不会提供有关该值的类型信息。
unknown：使用 unknown 类型时，TypeScript会提供类型检查信息，但在尝试访问其属性或执行操作之前，您必须明确指定类型或进行类型检查。
类型断言：
any：不需要类型断言，因为 any 类型是兼容的，可以赋给任何其他类型。
unknown：需要进行类型断言或类型检查，以告诉TypeScript您正在处理的确切类型。 示例：
```

TypeScript 中 unknown 和 any 区别？
- any 允许任意操作；
- unknown 更安全，不能直接进行操作，必须类型断言或类型缩小。

## 21. TypeScript 中如何实现类型守卫（Type Guards）？
```ts
function isString(value: unknown): value is string {
  return typeof value === 'string';
}
```

## type 是 类型别名，给一些类型的组合起别名，这样能够更方便地在各个地方使用
语法：
```js
interface Point {
  x: number;
  y: number;
}

type Point = {
  x: number;
  y: number;
};
```

type 可用于 string、number、bool、undefined、null
```js
// id 可以为字符串或数字，那么我们可以定义这么一个名为 ID 的 type
type ID = string | number;

// 定义一个名为 Circle 的对象结构 type：
type Circle = {
  x: number;
  y: number;
  radius: number;
}
```
